<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if locale == 'en' %}Reset Your Password{% else %}Passwort zurücksetzen{% endif %}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f9f9f9;
            padding: 30px;
            border: 1px solid #ddd;
            border-top: none;
        }
        .reset-button {
            display: inline-block;
            padding: 15px 30px;
            background-color: #007bff;
            color: white !important;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
            font-weight: bold;
            text-align: center;
        }
        .reset-button:hover {
            background-color: #0056b3;
        }
        .info-box {
            background: #d1ecf1;
            border: 1px solid #bee5eb;
            border-left: 4px solid #17a2b8;
            padding: 15px;
            margin: 20px 0;
        }
        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffc107;
            border-left: 4px solid #ffc107;
            padding: 15px;
            margin: 20px 0;
        }
        .security-details {
            background: #fff;
            padding: 15px;
            border-left: 4px solid #6c757d;
            margin: 20px 0;
            font-size: 0.9em;
            color: #666;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 0.9em;
        }
        .manual-link {
            word-break: break-all;
            color: #007bff;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>
            {% if locale == 'en' %}
                🔐 Password Reset Request
            {% else %}
                🔐 Passwort zurücksetzen
            {% endif %}
        </h1>
    </div>

    <div class="content">
        {% if locale == 'en' %}
            {# English Version #}
            <p>Hello {{ user.nameFirst ?? 'there' }},</p>

            <p>We received a request to reset the password for your account. Click the button below to create a new password:</p>

            <div style="text-align: center;">
                <a href="{{ reset_url }}" class="reset-button">Reset My Password</a>
            </div>

            <div class="info-box">
                <strong>ℹ️ Important Information:</strong><br>
                • This link will expire in <strong>{{ token_lifetime }} minutes</strong><br>
                • The link can only be used <strong>once</strong><br>
                • You'll be asked to create a new password
            </div>

            <p><strong>If the button doesn't work</strong>, copy and paste this link into your browser:</p>
            <p class="manual-link">{{ reset_url }}</p>

            <div class="security-details">
                <strong>Security Details:</strong><br>
                <strong>Request Time:</strong> {{ timestamp|date('Y-m-d H:i:s T') }}<br>
                <strong>IP Address:</strong> {{ ip_address }}<br>
                <strong>Device:</strong> {{ user_agent }}
            </div>

            <div class="warning-box">
                <strong>⚠️ Didn't request this?</strong><br>
                If you did not request a password reset, you can safely ignore this email. Your password will not be changed.
                However, someone may have your email address. Consider changing your password if you're concerned about security.
            </div>

        {% else %}
            {# German Version #}
            <p>Hallo {{ user.nameFirst ?? 'Benutzer' }},</p>

            <p>Wir haben eine Anfrage zum Zurücksetzen des Passworts für Ihr Konto erhalten. Klicken Sie auf die Schaltfläche unten, um ein neues Passwort zu erstellen:</p>

            <div style="text-align: center;">
                <a href="{{ reset_url }}" class="reset-button">Passwort zurücksetzen</a>
            </div>

            <div class="info-box">
                <strong>ℹ️ Wichtige Informationen:</strong><br>
                • Dieser Link läuft in <strong>{{ token_lifetime }} Minuten</strong> ab<br>
                • Der Link kann nur <strong>einmal</strong> verwendet werden<br>
                • Sie werden aufgefordert, ein neues Passwort zu erstellen
            </div>

            <p><strong>Falls die Schaltfläche nicht funktioniert</strong>, kopieren Sie diesen Link und fügen Sie ihn in Ihren Browser ein:</p>
            <p class="manual-link">{{ reset_url }}</p>

            <div class="security-details">
                <strong>Sicherheitsdetails:</strong><br>
                <strong>Anfragezeitpunkt:</strong> {{ timestamp|date('d.m.Y H:i:s T') }}<br>
                <strong>IP-Adresse:</strong> {{ ip_address }}<br>
                <strong>Gerät:</strong> {{ user_agent }}
            </div>

            <div class="warning-box">
                <strong>⚠️ Nicht angefordert?</strong><br>
                Falls Sie keine Passwortzurücksetzung angefordert haben, können Sie diese E-Mail ignorieren. Ihr Passwort wird nicht geändert.
                Allerdings könnte jemand Ihre E-Mail-Adresse haben. Erwägen Sie eine Passwortänderung, wenn Sie Sicherheitsbedenken haben.
            </div>

        {% endif %}
    </div>

    <div class="footer">
        <p>
            {% if locale == 'en' %}
                This is an automated security notification from Atlas.<br>
                Please do not reply to this email.
            {% else %}
                Dies ist eine automatische Sicherheitsbenachrichtigung von Atlas.<br>
                Bitte antworten Sie nicht auf diese E-Mail.
            {% endif %}
        </p>
    </div>
</body>
</html>
