<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if locale == 'en' %}Password Reset Successful{% else %}Passwort erfolgreich zurückgesetzt{% endif %}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #28a745;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f9f9f9;
            padding: 30px;
            border: 1px solid #ddd;
            border-top: none;
        }
        .success-box {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            border-left: 4px solid #28a745;
            padding: 15px;
            margin: 20px 0;
        }
        .security-details {
            background: #fff;
            padding: 15px;
            border-left: 4px solid #28a745;
            margin: 20px 0;
        }
        .security-details strong {
            color: #28a745;
        }
        .info-box {
            background: #d1ecf1;
            border: 1px solid #bee5eb;
            border-left: 4px solid #17a2b8;
            padding: 15px;
            margin: 20px 0;
        }
        .danger-box {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            border-left: 4px solid #dc3545;
            padding: 15px;
            margin: 20px 0;
        }
        .btn {
            display: inline-block;
            padding: 12px 24px;
            background-color: #28a745;
            color: white !important;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
        }
        .btn-danger {
            background-color: #dc3545;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>
            {% if locale == 'en' %}
                ✅ Password Successfully Reset
            {% else %}
                ✅ Passwort erfolgreich zurückgesetzt
            {% endif %}
        </h1>
    </div>

    <div class="content">
        {% if locale == 'en' %}
            {# English Version #}
            <p>Hello {{ user.nameFirst ?? 'there' }},</p>

            <div class="success-box">
                <strong>✓ Your password has been successfully reset!</strong><br>
                You can now log in to your account with your new password.
            </div>

            <p>This email confirms that your password was changed using the password reset feature.</p>

            <div class="security-details">
                <strong>Security Details:</strong><br>
                <strong>Date & Time:</strong> {{ timestamp|date('Y-m-d H:i:s T') }}<br>
                <strong>IP Address:</strong> {{ ip_address }}<br>
                <strong>Device:</strong> {{ user_agent }}
            </div>

            <div class="info-box">
                <strong>ℹ️ What happens next:</strong><br>
                • All your active sessions on all devices have been logged out for security<br>
                • You'll need to log in again with your new password<br>
                • Any previously issued password reset links have been invalidated
            </div>

            <div style="text-align: center;">
                <a href="{{ login_url }}" class="btn">Log In Now</a>
            </div>

            <div class="danger-box">
                <strong>🔒 Wasn't you?</strong><br>
                If you did not reset your password, your account may be compromised.
                <strong>Please contact support immediately and change your password again.</strong>
                <br><br>
                <a href="mailto:support@example.com" class="btn btn-danger">Report Unauthorized Reset</a>
            </div>

            <p><strong>Security Tip:</strong> Make sure your new password is strong and unique. Don't use the same password across multiple services.</p>

        {% else %}
            {# German Version #}
            <p>Hallo {{ user.nameFirst ?? 'Benutzer' }},</p>

            <div class="success-box">
                <strong>✓ Ihr Passwort wurde erfolgreich zurückgesetzt!</strong><br>
                Sie können sich jetzt mit Ihrem neuen Passwort in Ihr Konto einloggen.
            </div>

            <p>Diese E-Mail bestätigt, dass Ihr Passwort über die Passwort-Zurücksetzen-Funktion geändert wurde.</p>

            <div class="security-details">
                <strong>Sicherheitsdetails:</strong><br>
                <strong>Datum & Uhrzeit:</strong> {{ timestamp|date('d.m.Y H:i:s T') }}<br>
                <strong>IP-Adresse:</strong> {{ ip_address }}<br>
                <strong>Gerät:</strong> {{ user_agent }}
            </div>

            <div class="info-box">
                <strong>ℹ️ Was passiert als Nächstes:</strong><br>
                • Alle Ihre aktiven Sitzungen auf allen Geräten wurden aus Sicherheitsgründen abgemeldet<br>
                • Sie müssen sich mit Ihrem neuen Passwort erneut anmelden<br>
                • Alle zuvor ausgestellten Passwort-Zurücksetzen-Links wurden ungültig gemacht
            </div>

            <div style="text-align: center;">
                <a href="{{ login_url }}" class="btn">Jetzt anmelden</a>
            </div>

            <div class="danger-box">
                <strong>🔒 Waren Sie das nicht?</strong><br>
                Falls Sie Ihr Passwort nicht zurückgesetzt haben, könnte Ihr Konto kompromittiert sein.
                <strong>Bitte kontaktieren Sie umgehend den Support und ändern Sie Ihr Passwort erneut.</strong>
                <br><br>
                <a href="mailto:support@example.com" class="btn btn-danger">Unautorisierte Zurücksetzung melden</a>
            </div>

            <p><strong>Sicherheitstipp:</strong> Stellen Sie sicher, dass Ihr neues Passwort stark und einzigartig ist. Verwenden Sie nicht dasselbe Passwort für mehrere Dienste.</p>

        {% endif %}
    </div>

    <div class="footer">
        <p>
            {% if locale == 'en' %}
                This is an automated security notification from Atlas.<br>
                Please do not reply to this email.
            {% else %}
                Dies ist eine automatische Sicherheitsbenachrichtigung von Atlas.<br>
                Bitte antworten Sie nicht auf diese E-Mail.
            {% endif %}
        </p>
    </div>
</body>
</html>
